Do the smallest schema-loader change only. Do not add more ITs or convert more tables.

Context: pilot *IT is green. Shared H2, Ant forkmode=once, one JVM. ~100 Oracle tables exist in prod; we will NOT load them. H2 is a SQL-parse + fillEntity() harness, not Oracle.

Goal:
1. Split the current monolithic IT schema into per-table files under resources/tables/ with a numeric load-order prefix (match asap-common/General/Migrations/database/ style).
2. Update H2IntegrationTestBase to load every resources/tables/*.sql once in @BeforeClass, sorted by filename.
3. Move the existing SPT_COUNTRY_INFO DDL out of asapgov-it-schema.sql into resources/tables/010_SPT_COUNTRY_INFO.sql and remove the old single-file fixture if nothing else remains in it.

Constraints:
- No per-class table allowlists.
- DDL only in resources/tables/. No seed INSERTs in those files.
- Do not invent FK ordering beyond the numeric prefix. One table today.
- Do not translate the rest of the Oracle schema.
- Do not switch to Testcontainers.
- Keep existing pilot IT behavior identical.

Use @ on H2IntegrationTestBase, asapgov-it-schema.sql, and the pilot *IT class. Show the diff first, then make the change.



Add one integration test for whatever the next high-risk ResultSet class is).

Smallest change:
- Read that class and name every table it queries.
- Add only those tables as resources/tables/0xx_TABLE.sql (numeric prefix after existing files). H2 DDL only.
- New *IT extending H2IntegrationTestBase: insert minimal rows, call the production method, assert fillEntity() fields.
- Do not add other ResultSets, allowlists, or seed files.
- Show the table list and diff before editing.
